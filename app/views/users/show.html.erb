<h1>Email: <%= @user.email %></h1>
<h2><%= link_to 'Create Post', new_user_post_path(@user.id) %></h2>
<% @user.posts.each do |post| %>
  <ul>
    <li>
      <h2> <%= post.title %> </h2>
      <p> <%= post.body %> </p>
    </li>
  </ul>
<% end %>
